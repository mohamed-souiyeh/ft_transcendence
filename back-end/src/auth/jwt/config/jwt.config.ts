// import { config } from "dotenv";

// config({
//   encoding: 'latin1',
//   debug: true,
//   override: false,
// });

export const jwt_expire_time = {
  expiresIn: 60 * 60 * 12, // available for 12hrs
};
